# Project Task Tracker (Task.md)

This file tracks all agreed tasks between the user and the agent, showing their status and final outcomes.

| Tên Task | Ngày Tháng Giờ | Trạng Thái / Output Cuối Cùng |
| :--- | :--- | :--- |
| Thiết lập Task.md và tạo Custom Skill `task-manager` | 2026-06-20 13:40 | Đã xong (Tạo file Task.md và skill `task-manager` tại `.agents/skills/`) |
| Loại bỏ mode `voice`, thêm `live session` theo gói và điều phối follow-up/next question | 2026-06-20 14:20 | Đã xong (Loại bỏ lựa chọn voice khỏi luồng tạo session, thêm live session và khóa cho Basic/Free bằng redirect sang upgrade; cập nhật FE/BE/test để chỉ còn camera + live session ở mode selection, giữ backend chặn live nếu không phải Pro/Premium) |
| Đổi tiêu đề trang sessions sang `Lịch sử` và sửa render `Overall Presentation Score` | 2026-06-20 15:05 | Đã xong (Đổi nhãn header sessions sang `Lịch sử`; siết extractor để lấy đúng điểm explicit `Điểm: 65/100` thay vì nhầm `100%`; render score block compact hơn với `Cộng` / `Trừ` in đậm; cập nhật prompt backend và test FE) |
| Dọn cleanup còn sót: bỏ fallback `video` ở sessions, scrub copy `voice`, và fail-fast khi migration lỗi | 2026-06-20 15:15 | Đã xong (Sessions chỉ còn filter camera/live; FAQ không còn nhắc voice mode; backend startup sẽ dừng nếu migration lỗi thay vì nuốt lỗi) |
| Chuẩn bị cấu hình đăng nhập GitHub | 2026-06-21 03:43 | Đã xong (Xác nhận flow GitHub OAuth đã có sẵn ở FE/BE, thêm hướng dẫn cấu hình callback và tạo `BE/.env.example` với `GITHUB_CLIENT_ID` / `GITHUB_CLIENT_SECRET`) |
| Xóa toàn bộ liên quan GitHub | 2026-06-21 03:44 | Đã xong (Đã xóa GitHub OAuth route, nút GitHub trên FE, biến env và copy liên quan; giữ lại Google OAuth và xác nhận auth test pass) |
| Thêm coverage matrix và test contract cho auth/session/billing | 2026-06-21 05:54 | Đã xong (Thêm plan/matrix FE↔BE, test BE cho auth login, live-mode session gating, PayOS checkout và test FE cho Google OAuth buttons; regression set auth/session/billing pass) |
| Mở rộng coverage cho profile upload/download và admin invite/access | 2026-06-21 06:11 | Đã xong (Thêm test FE cho profile avatar/resume và admin invite/revoke/remove, thêm test BE cho avatar upload/delete và admin invite/access; regression bundle profile/admin/auth/session/billing pass) |
| Dọn warnings test còn lại | 2026-06-21 06:13 | Đã xong (Đổi `UserOut` sang `ConfigDict`, thêm future flags cho router test wrappers, mock flat cho chart tests và stub các API phụ trợ; FE bundle 11 files pass, backend bundle pass) |
| Xóa sạch dữ liệu user/self-delete, đồng bộ pricing sheet, và thêm bước chọn ngôn ngữ ở wizard session | 2026-06-22 04:17 | Đã xong (Tạo helper xóa account dùng chung cho admin/self-delete; thêm `language` vào session tạo/xem/chấm và bỏ chọn ngôn ngữ trong camera; dựng lại pricing sheet dạng bảng so sánh dùng chung cho landing/upgrade; FE/BE tests và FE build pass) |
| Polish pricing mobile sheet theo hướng bảng hơn | 2026-06-22 04:53 | Đã xong (Mobile pricing được chỉnh sang sheet-style rõ hơn: header tách lớp, hàng summary dạng lưới, highlight xếp dọc gọn hơn và CTA nằm ở footer để nhìn như bảng so sánh) |
| Thêm sticky header cho mobile pricing plan | 2026-06-22 04:55 | Đã xong (Sticky plan header trên mobile pricing giữ tên gói, CTA context và trạng thái phổ biến/gói hiện tại khi người dùng cuộn qua từng plan) |
| Làm mobile pricing compact hơn | 2026-06-22 04:57 | Đã xong (Giảm padding, nén summary rows, thu nhỏ typography và ẩn highlights ở màn rất nhỏ để mobile pricing hiển thị được nhiều plan hơn trong một viewport) |
| Thêm backend bootstrap và regression coverage cho automation tests | 2026-06-22 12:36 | Đã xong (Thêm endpoint bootstrap automation được guard bằng `TEST_AUTOMATION_ENABLED`, wire vào config/main mặc định tắt ở production, bổ sung smoke/regression test cho auth/session/export/billing/admin và service regressions cho scoring, transcript cleanup, adaptive follow-up, payment order cleanup) |
| Build browser smoke flows cho public/auth/candidate/interview/billing/admin | 2026-06-22 12:55 | Đang sửa dở (Đã thêm `FE/e2e/smoke/*.spec.ts` và `FE/e2e/support/{browser,bootstrap,selectors}.ts`, nhưng local bootstrap endpoint đang timeout trên backend nên chưa thể hoàn tất chạy smoke end-to-end; cần làm sạch contract bootstrap/DB hoặc chờ backend ổn định rồi rerun Playwright) |
| Kiểm tra log session `b547d21d-6044-4309-afbf-43ccef5fcded` và nguyên nhân STT timeout | 2026-06-22 23:51 | Đã xong (Đối chiếu log `/var/log/syslog`, DB session/answers và code STT; xác nhận lỗi là timeout ở `process_video_answer_background` khi `asyncio.wait_for` bao Whisper medium 180s, không phải lỗi chọn ngôn ngữ; có thêm một lần `answer-video` 400 sau khi session đã complete) |
| Tối ưu interview room: bỏ realtime transcript, chỉ transcribe sau khi dừng | 2026-06-22 23:51 | Đã xong (Gỡ toàn bộ realtime STT khỏi camera interview flow ở FE/BE, giữ upload video/audio rồi transcribe một lần sau khi dừng; cập nhật telemetry lưu lại sau transcribe, xoá websocket STT route và test liên quan, FE build và backend STT tests pass) |
| Sửa 500 khi tạo phiên phỏng vấn do startup migration chạy nền | 2026-06-23 00:12 | Đã xong (Đổi startup migration sang chạy đồng bộ trước khi backend nhận traffic; backend chỉ báo ready sau khi hoàn tất 023 migrations, tránh 500/timeout ở `create_session` và `catalog`) |
| Sửa thiếu điểm khi end session ngay sau 1 câu vì upload/chấm điểm còn pending | 2026-06-23 00:23 | Đã xong (FE chờ upload answer đang chạy trước khi complete, BE chờ `PENDING` answers xong mới finalize session và tính `avg_score`; đồng thời bỏ framing khỏi scoring/report và sửa `recordingDurationSec` theo timestamp thực để `Speaking Pace` backend tính đúng từ transcript) |
| Cho end session thoát ngay và hiển thị popup chờ chấm điểm ở Sessions | 2026-06-23 01:00 | Đang thực hiện... (Thiết kế luồng `End` fire-and-forget, chuyển thẳng về `Sessions`, và dùng state pending tạm để hiện modal “Hãy chờ chút Invera chấm điểm cho bạn” cho tới khi session hoàn tất) |
| Đổi redeem code sang UUID one-time có hạn dùng và thêm tab admin sinh code | 2026-06-24 03:31 | Đang thực hiện... (Chuyển redeem sang mã UUID lưu trong DB, hỗ trợ hết hạn 7 ngày / 30 ngày / ngày cụ thể, chặn tái sử dụng, và thêm tab admin để tạo code theo từng gói basic/pro/premium) |
